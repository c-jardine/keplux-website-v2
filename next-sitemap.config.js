/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://www.keplux.com',
  generateRobotsTxt: true, // (Parameter for creating robots.txt file)
};

module.exports = config;
